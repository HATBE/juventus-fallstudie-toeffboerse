import { SwissNumberFormatPipe } from './swiss-number-format.pipe';

describe('SwissNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SwissNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
